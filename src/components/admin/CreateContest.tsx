import {ReactElement} from "react";

const CreateContest = () : ReactElement => {
    return (
        <>
            <h1>Pagina para crear un contest, añadiendo cuantas preguntas, colores, participantes y eso</h1>
        </>
    );
}

export default CreateContest;