import {ReactElement} from "react";

const AdminPage = () : ReactElement => {
    return (
        <>
            <h1>Pagina del admin para crear o terminar un contest</h1>
        </>
    );
}

export default AdminPage;